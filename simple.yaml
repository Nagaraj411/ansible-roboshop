- name: configuration frontend server
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: Disable the default nodejs module
    ansible.builtin.command: dnf module disable nginx -y
  
  - name: enable nginx
    ansible.builtin.command: dnf module enable nginx:1.24 -y